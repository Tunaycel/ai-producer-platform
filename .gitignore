# Environment variables & secrets
.env
.env.local
*.pem

# Dependencies
node_modules/
__pycache__/
*.pyc
venv/
.venv/

# Build & OS outputs
dist/
build/
.DS_Store
Thumbs.db

# Audio caches / temporal files
*.wav
*.mp3
!examples/*.mp3
temp_stems/

# Claude Code local session state (not the agents folder — that stays untracked
# but visible in the editor; only add specific noisy files here as needed)
.claude/scheduled_tasks.lock

# Local sqlite databases (dev fallback DB, test DB)
*.db
